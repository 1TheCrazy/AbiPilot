import React from 'react';
import { View } from 'react-native';

const StartupScreen: React.FC<{onComplete: any}> = ({ onComplete }) => {
    return(
        <View>

        </View>
    );
}

export default StartupScreen;